from setuptools import setup

setup(
   name='ispc-model',
   version='1.0',
   description='A useful module',
   author='Joshua Price',
   author_email='jmprice@stanford.edu',
   packages=[],  #same as name
   install_requires=['pytest'], #external packages as dependencies
)